# EduAgent 演示 PPT 生成提示词（前端真实功能模块版）

---

## 角色设定

为 **EduAgent（r436-runtime-kit）—— 基于 LangGraph 多智能体协同的自适应学习系统** 制作竞赛答辩 PPT。

受众：竞赛评委。策略：**上半场按用户实际看到的界面展示功能模块（配截图），下半场展示技术架构**。

---

## 一、总体规格

| 项目 | 要求 |
|------|------|
| 页数 | 16-18 页 |
| 配色 | 深蓝（#1a365d / #2b6cb0）主色 + 暖橙（#ed8936）点缀 |
| 布局 | 每页左栏文字说明 + 右栏截图或架构图占位 |
| 命名 | 标题用 **EduAgent**，页脚小字 `r436-runtime-kit` |

### 截图占位符格式

```
┌───────────────────────────────────────┐
│  📷  请截取：[页面路径]                │
│  [描述要展示的操作和内容]              │
│  尺寸：1920×1080                       │
└───────────────────────────────────────┘
```

---

## 二、上半场：用户视角功能模块（第 1-10 页）

### 第 1 页 · 封面

**标题**：EduAgent — 基于 LangGraph 多智能体协同的自适应学习系统

**副标题**：从"千人一面"到"一人一策"

**底部**：团队 · 日期 · r436-runtime-kit

> 背景：系统首页截图暗化

---

### 第 2 页 · 学生端功能总览（导航页）

**标题**：学生端十大功能模块

**列出学生侧边栏实际显示的 10 个菜单项**（就是 `ConsoleSidebar.tsx` 中学生角色看到的），每项配图标 + 一句话 + 对应页码：

| 菜单项 | 路由 | 一句话 | 详见 |
|--------|------|--------|------|
| 🏠 学习中心 | `/` | 统计卡片 + 每日任务 + 学科管理 + 快捷入口 | 第3页 |
| 👤 学习画像 | `/profile` | 7维画像展示 + 手动编辑 + 对话同步 | 第4页 |
| 🗺️ 学习路径 | `/path` | 阶段→章节→按天分组 + 任务卡片 + 推荐资源 | 第5页 |
| 🧠 知识图谱 | `/kg` | AntV G6 交互图 + 搜索筛选 + 节点详情 | 第6页 |
| 📚 资源库 | `/resources` | 筛选浏览 + 资源详情 + 内联测验 + 导出 | 第7页 |
| 💬 智能对话 | `/chat` | SSE流式 + 多模态输入 + Agent流水线可视化 | 第8页 |
| ⚙️ 资源生成 | `/generate` | 选类型 + 写需求 → 批量生成 + 实时进度 | 第9页 |
| ✏️ 练习中心 | `/practice` | 首页统计/测验/考试/历史四个视图 | 第10页 |
| 📊 学习分析 | `/analytics` | 学习路径页（含分析数据）| — |
| ⚙️ 系统设置 | `/settings` | AI模型配置 + 偏好 + 数据管理 | — |

> 注：学习分析和系统设置在此页提一嘴即可，不单独成页

---

### 第 3 页 · 功能模块：学习中心首页（Home）

**路由**：`/`

**左栏（用户看到什么、能做什么）**：

**欢迎横幅**：
- 渐变蓝色卡片，显示"连续学习 X 天"、"欢迎回来，[名字]！"
- 学生看到"开始学习"按钮（→跳转对话页）
- 教师看到"班级管理"按钮（→跳转教师页）

**统计卡片（4 个）**：
- 我的科目数 / 浏览资源数 / 累计学习小时 / 测验正确率 %

**7 日学习趋势图**：SVG 迷你柱状图，今天的数据柱高亮标注数值

**学科管理区**：
- 列出我创建的科目和加入的班级科目，每个显示名称 + 进度条 + 类型标签
- "新建科目"按钮 → 弹窗输入名称 + 可选上传 PDF 教材
- "加入班级"按钮 → 弹窗输入教师给的邀请码
- 鼠标悬停科目出现删除按钮

**每日任务面板**（仅学生）：今日待完成任务 + 完成数统计

**快捷入口卡片（3 个）**：生成学习资源 / 更新学习画像 / 浏览资源库

> **【截图 A】**
> 请截取：Home 首页（`/`）
> 展示：统计卡片 + 每日任务面板 + 学科列表 + 快捷入口卡片（需有数据的状态）
> 尺寸：1920×1080

---

### 第 4 页 · 功能模块：学习者画像（ProfilePage）

**路由**：`/profile`

**左栏（用户看到什么、能做什么）**：

**顶部横幅**：当前学科名称、学习目标、每日学习时长、截止日期、画像完整度百分比

**"学习情境与偏好"区域（7 个可编辑字段）**：
- 学习目标 / 时间安排 / 专业背景 / 学习历史 / 已有经验 / 内容偏好 / 资源偏好
- 鼠标悬停出现编辑按钮 → 点击可内联修改
- "从当前对话同步画像"按钮 → 触发后台工作流（有进度条）
- 同步后展示预览：新增/更新/冲突/忽略，用户确认后应用

**"画像事实"区域**：
- 展示所有从对话中推断的事实，每条标注来源（对话推断/学生自述/行为观察）
- 每条可：修改 / 停用个性化 / 删除

**"当前学习状态"区域**：
- 兴趣自评滑块（1-5）+ AI 辅助校准（系统估计值 + 置信度 + 证据来源）
- 学科能力维度卡片：标签 + 状态 + 置信度 + 推荐下一步行动

**"知识点掌握与证据"区域**：
- 每个知识点卡片：掌握状态 + 证据来源统计（对话/诊断/练习/行为各多少次）

> **【截图 B】**
> 请截取：ProfilePage 画像页（`/profile`）
> 展示：顶部横幅 + "学习情境与偏好"区（部分字段有值）+ "知识点掌握"区
> 尺寸：1920×1080

---

### 第 5 页 · 功能模块：学习路径（LearningPathPage）

**路由**：`/path`

**左栏（用户看到什么、能做什么）**：

**三栏布局**（有路径数据时）：

**左栏 — 阶段/天列表**：
- 可展开的阶段列表，每个阶段包含若干天
- 每天显示天序号 + 标题 + 进度环 + 已完成/总数

**中栏 — 当天任务卡片**：
- 每张卡片显示：状态图标（✓已完成 / 旋转中·进行中 / 🔒锁定 / 🎯待开始）
- 任务标题 + 目标描述
- 类型标签（阅读/推演/小测/专项/方法/模拟/复盘）带图标
- 预计时间
- "⚡ AI 注入"标记（AI 动态插入的任务）
- "🔒 未解锁"标记
- 操作按钮："开始"/"继续 →"/"✓ 完成"/"🔒 锁定"
- Tab 切换："任务" / "推荐资源"（AI 推荐的外部学习资源链接）

**右栏**：
- "立即开始"卡片（下一个待完成任务，渐变头部）
- "学习分析"统计（已完成任务数、阶段进度、当前阶段）
- "练习"入口 → 跳转练习中心 + "生成题集"按钮

**空状态时**："开始你的学习路径"引导页 → 点击跳转对话规划模式

**规划向导**（弹窗）：多步骤表单引导创建学习计划

> **【截图 C】**
> 请截取：LearningPathPage 学习路径页（`/path`）
> 展示：左栏阶段列表展开 + 中栏任务卡片（多种类型可见）+ 右栏统计
> 尺寸：1920×1080

---

### 第 6 页 · 功能模块：知识图谱（KnowledgeGraphPage）

**路由**：`/kg`

**左栏（用户看到什么、能做什么）**：

**顶部操作栏**：
- 搜索输入框（按知识点名称搜索）
- 布局切换按钮：力导向 / 层级 / 环形（三种布局）
- 筛选下拉：按状态（未开始/进行中/已掌握/需复习）+ 按章节

**统计条**：节点数 / 边数 / 已掌握数 / 进行中数 / 未开始数（带颜色圆点）

**主区域 — AntV G6 交互图**：
- 节点表示知识点，边表示前置依赖关系
- 节点颜色编码：🟢已掌握 / 🔵进行中 / ⚪未开始
- 支持拖拽平移、滚轮缩放

**浮层卡片**（点击节点）：
- 节点名称 + 状态 + 描述
- "去学习"按钮 → 跳转资源库（按该知识点筛选）

**右侧详情面板**（展开后）：
- 完整节点信息 + 关联资源列表 + 相关知识点

> **【截图 D】**
> 请截取：KnowledgeGraphPage 知识图谱页（`/kg`）
> 展示：力导向图 + 多种颜色节点 + 顶部筛选栏 + 浮层卡片
> 尺寸：1920×1080

---

### 第 7 页 · 功能模块：资源库（ResourceLibrary）

**路由**：`/resources`

**左栏（用户看到什么、能做什么）**：

**三个 Tab**：
- "我的资源"：已生成的学习资源
- "推荐资源"：AI 基于画像推荐的资源
- "联网搜索"：在线搜索外部学习资源

**"我的资源" 视图**：

**教材卡片**（如已上传教材）：书名 + 页数 + 章节数 → 点击浏览

**筛选栏**：类型下拉 / 难度 / 搜索关键词 / 排序（推荐/最新/最简单/最困难）

**网格/列表切换按钮**

**资源卡片**（网格或列表模式）：
- 每张卡片：类型图标 + 标题 + 描述 + 难度标签 + 质量标签 + 时长 + 章节
- 书签按钮 + 完成勾选框
- 悬停出现删除按钮

**点击进入资源详情**：
- 头部：图标 + 标题 + 难度/类型/质量标签 + 时长 + 来源 + 版本号
- 正文区：Markdown 渲染（含 KaTeX 公式 + Mermaid 图表）
- 测验类资源：内联答题（选项按钮 + 提交 → 即时结果）
- 操作按钮：书签 / 标记完成 / 重新生成 / 反馈
- 关联资源推荐
- 知识图谱预览

**批量操作模式**：勾选多个资源 → 批量标记/书签/导出

> **【截图 E】**
> 请截取：ResourceLibrary 资源库（`/resources`）
> 展示：网格视图 + 筛选栏 + 多个资源卡片（不同类型）+ 书签/完成状态
> 尺寸：1920×1080

---

### 第 8 页 · 功能模块：智能对话 —— 万能调用入口（ChatPage）

**路由**：`/chat`

**左栏（用户看到什么、能做什么）**：

**三种模式（Tab 切换）**：
- **自由对话**：开放式 AI 交流，系统自动判断意图并调用相关 Agent
- **调用模式**：用户通过快捷指令或自然语言，直接触发指定 Agent 执行任务——可调用的 Agent 包括：
  - 🧠 生成画像（ProfileAgent）
  - 🔍 检索知识（KnowledgeAgent）
  - 🔬 诊断分析（DiagnosisAgent）
  - ✏️ 生成试题（QuestionAgent）
  - 📋 规划路径（PlannerAgent）
  - ⚙️ 生成资源（ResourceAgent）
  - ✅ 检查质量（ReviewAgent）
  - 📝 批改作答（GradingAgent）
  - 🎬 多模态处理（MultimodalAgent）
- **规划模式**：多轮引导式对话，收集学习信息后生成学习路径

**快捷指令（一键触发 Agent）**：
- "了解我的基础" → 触发 ProfileAgent 构建画像
- "诊断薄弱点" → 触发 DiagnosisAgent 自适应诊断
- "规划学习路径" → 触发 PlannerAgent 生成学习规划
- "生成练习题" → 触发 QuestionAgent 出题
- "生成思维导图" → 触发 ResourceAgent + Markmap 渲染
- "讲解知识点" → 触发 KnowledgeAgent 检索 + LLM 讲解

**Agent 流水线进度条**：
- 消息流中实时显示：当前运行的 Agent 名称 + 进度百分比
- 动态流水线步骤（圆形节点 + 连接线）：蓝色脉冲=进行中 / 绿色勾=已完成 / 灰色=等待中
- 全部完成后显示绿色"生成完成"横幅 + 跳转按钮（→画像/路径/资源库）

**生成资源在对话中内联展示（MultimodalResultView 卡片）**：
- 🧠 **思维导图**：交互式 SVG（Markmap 渲染），480px 高度可滚动
- ✏️ **练习题**：题目列表卡片，每道题展开显示选项 + 答案 + 解析 + 知识点标签
- 🃏 **闪卡**：折叠式卡片，点击翻转显示正面/背面/提示
- 🖼️ **AI 生成图解**：图片缩略图直接展示
- 🎬 **视频脚本**：代码块展示脚本内容
- 📋 **学习路径预览**：阶段时间线（彩色左边框 + 标题 + 目标 + 预计时间）
- 🔄 **变式题**：可折叠的变式题目详情面板

**输入区**：图片上传 + 语音输入（科大讯飞 ASR）+ 文本输入 + 发送/停止
**底部开关**：联网搜索 / 深度思考
**侧边栏**：左侧会话历史（可切换/删除/重命名）/ 右侧画像面板（规划模式）
**全局悬浮**：任意页面右下角可弹出迷你对话面板

> **【截图 F】（建议两张并列）**
> 左：ChatPage 调用模式——展示一段对话："生成思维导图"→ Agent 流水线进度条"生成资源 ✓ → 检查质量 ···"→ 思维导图卡片内联展示
> 右：ChatPage 生成练习题——展示题目列表卡片 + 展开一道题的选项/答案/解析
> 尺寸：各 900×600

---

### 第 9 页 · 功能模块：资源生成（ResourceGenerationPage）

**路由**：`/generate`

**左栏（用户看到什么、能做什么）**：

**需求输入区**：文本框描述想生成什么（支持 Markdown，最多 500 字）

**资源类型选择（8 种，最多选 3 种）**：
| 类型 | 图标 | 说明 |
|------|------|------|
| 学习文档 | 📄 | 结构化知识讲解 |
| 思维导图 | 🧠 | 知识结构图（Markmap 渲染） |
| 练习题库 | ✏️ | 含答案和解析的自测题 |
| 实操案例 | 💻 | 含代码示例的实践任务 |
| 拓展阅读 | 📖 | 学术风格延伸阅读 |
| 知识图解 | 🖼️ | AI 生成的教学插图 |
| PPT 演示 | 📊 | 可下载的演示文稿 |
| 动画演示 | 🎬 | 需本地 Manim 渲染环境 |

**PPT 模板选择**（选中 PPT 时出现）：模板缩略图网格 + "智能推荐"选项

**生成按钮**：显示"开始生成（N 种资源）"

**实时进度区**：
- 每种资源独立进度条：类型名 + 状态图标（旋转中/✓完成/✗失败）+ 阶段标签 + 耗时
- 失败的任务显示"重试"按钮
- 完成的任务显示"查看资源"→ 跳转资源库
- 支持中途取消

> **【截图 G】（可左右两张并列）**
> 左：ResourceGenerationPage 输入需求 + 选择类型
> 右：ResourceGenerationPage 多个并行生成进度
> 尺寸：各 900×600

---

### 第 10 页 · 功能模块：练习中心（PracticePage）

**路由**：`/practice`

**左栏（用户看到什么、能做什么）**：

**四个视图（Tab 切换）**：

**① 首页（统计面板）**：
- 正确率 % / 答题总量 / 薄弱点数量 / 7 日活跃趋势图
- 快捷入口卡片：快速诊断 · 自定义练习 · 错题复习 · 套卷练习
- 教师推送的练习组（如已加入班级）

**② 测验（逐题作答）**：
- 题目头部：题号 + 题型标签 + 难度 + 知识点
- 题干（Markdown 渲染）
- 答题区：选择按钮（选择题）/ 文本框（填空/简答）
- 提交批改 → 即时显示：分数 + 对/错 + 错误类型标签 + 解析 + 建议
- 导航按钮：上一题 / 下一题 / 重做

**③ 考试（题集模式）**：
- 题集详情：标题 + 题数 + 时间 + 总分
- "开始作答"/"继续作答"按钮
- 提交全部 → 总分环形图 + 薄弱知识点列表
- 作答记录表格（每次尝试的日期/状态/分数）

**④ 历史（错题本+记录）**：
- 左侧错题本：薄弱题目列表，每道显示"你的答案 vs 正确答案" + 错误类型标签
- 右侧答题历史：按时间排列，每条显示题目摘要 + 得分 + 日期

> **【截图 H】**
> 请截取：PracticePage 测验视图（`/practice`）
> 展示：一道题 + 批改结果（含分数、错误类型标签："概念错误"/"计算错误"等、解析、建议）
> 尺寸：1920×1080

---

### 第 11 页 · 功能模块：讲稿学习（LecturePage）+ 教师端 + 管理后台

**本页简要展示剩余功能模块**（3 栏或上下排列，非重点但需要让评委知道系统完整性）

**讲稿学习页（`/lecture`）**：
- 左侧章节大纲（带状态指示器：未开始/进行中/已掌握/需复习）
- 主区域三种模式切换：📖讲稿 / 🃏闪卡（翻转）/ 🪜分步推演
- Markdown 正文 + Mermaid 图表渲染 + 内联测验
- 小节资源工作区 + 上一节/下一节导航

**教师端（`/teacher`）**：
- 班级卡片列表（名称 + 邀请码可复制 + 学生数）
- 班级详情：练习推送（从题库选题或新建题目）+ 学生管理 + 学情统计

**管理后台（`/admin`）**：
- 题库管理（CRUD + 审核流程：待审→通过/拒绝）+ 知识图谱管理 + 数据统计 + 系统配置

> **【截图 I】**
> 请截取：LecturePage 讲稿页（`/lecture`）讲稿模式 + 侧边栏展开
> 尺寸：1920×1080

---

## 三、下半场：技术架构（第 12-16 页）

### 第 12 页 · 技术架构总览

**标题**：三层架构 + 10 Agent LangGraph 协同引擎

**全部为架构图**（draw.io / Excalidraw 绘制）：

```
┌──────────────────────────────────────────────────────────────────────┐
│                    前端层 (React 19 + TypeScript + Vite)             │
│                                                                      │
│  Home│Chat│Path│KG│Resources│Profile│Practice│Settings│Teacher│Admin│
│  Zustand(7Store)│React Router│Tailwind│ECharts│AntV G6│Mermaid│KaTeX │
│  markmap│react-markdown│科大讯飞 ASR (WebSocket)                      │
├──────────────────────────────────────────────────────────────────────┤
│                    网关层 (FastAPI + Uvicorn + SQLite)                │
│                                                                      │
│  17+ 路由 │ SSE Stream │ JWT + 4角色鉴权(学生/教师/管理/家长)        │
│  Request-ID 全链路追踪 │ 每用户 AI 凭证上下文绑定                     │
├──────────────────────────────────────────────────────────────────────┤
│                    智能体层 (LangGraph StateGraph)                    │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ ConversationAgent — 外层调度                                    │   │
│  │ 意图分类 · 动态人设(4阶段) · 间接探测(6策略) · 事实抽取        │   │
│  └───────────────────────────┬──────────────────────────────────┘   │
│                              │                                       │
│  ┌──────────┐ ┌──────────────┴─┐ ┌──────────────┐                   │
│  │Profile   │ │LangGraph       │ │Knowledge     │                   │
│  │7维画像   │ │Orchestrator    │ │RAG语义检索   │                   │
│  └────┬─────┘ │11节点·条件路由 │ └──────┬───────┘                   │
│       │       └───────┬────────┘        │                           │
│       ▼               ▼                 ▼                           │
│  ┌──────────┐ ┌──────────────┐ ┌──────────────┐                     │
│  │Diagnosis │ │PlannerAgent  │ │ResourceAgent │                     │
│  │自适应诊断│ │3模式+4级降级 │ │6类资源+修复  │                     │
│  └────┬─────┘ └──────┬───────┘ └──────┬───────┘                     │
│       ▼               ▼               ▼                             │
│  ┌──────────┐ ┌──────────────┐ ┌──────────────┐                     │
│  │Question  │ │GradingAgent │ │ReviewAgent   │                     │
│  │5题型+变式│ │5类错误分类  │ │7维审核+重试  │                     │
│  └──────────┘ └──────────────┘ └──────┬───────┘                     │
│                                       │                             │
│                          ┌────────────┴──────────┐                  │
│                          │MultimodalAgent        │                  │
│                          │12类多模态任务路由      │                  │
│                          └───────────────────────┘                  │
├──────────────────────────────────────────────────────────────────────┤
│                    基础设施                                           │
│  RAG(FAISS+LlamaIndex+text2vec)│LLM工厂(DeepSeek默认)│多源搜索+熔断 │
│  内容安全(pyahocorasick)│会话状态持久化│AssessmentLoop│工作流管理     │
└──────────────────────────────────────────────────────────────────────┘
```

---

### 第 13 页 · 技术架构：LangGraph 编排 + 10 Agent 详解

**标题**：LangGraph StateGraph —— 11 节点指挥 10 个 Agent

**上半部分：StateGraph 结构图**（Mermaid 渲染）

```
intent_router
  ├→ conversation → reply (chat)
  ├→ profile (画像)
  ├→ knowledge (知识检索)
  ├→ diagnosis (诊断)
  ├→ profile → planner (规划)
  ├→ question (出题)
  ├→ grading (批改)
  ├→ profile → diagnosis → resource → multimodal (辅导)
  ├→ grading → diagnosis → profile → planner → resource (评估闭环)
  └→ profile → knowledge → diagnosis → planner → resource → review (全流程)
       review → 不通过(≤2次) → resource (重试)
              → 通过 → reply → END
```

**下半部分：10 Agent 职责表**

| Agent | 代码量 | 一句话 |
|-------|--------|--------|
| ConversationAgent | 60KB | 意图分类(XML标签) + 动态人设(4阶段) + 间接探测(6策略) + 事实抽取 |
| ProfileAgent | 29KB | 7维画像构建 + 增量更新(fact-dirty) + Rich Facts话题级画像 |
| KnowledgeAgent | 5KB | RAG语义检索 → 课程目录回退 → LLM查询扩展 |
| DiagnosisAgent | 82KB | 三级自适应 + 掌握度矩阵 + 动态阈值 + 衰减检测 + 艾宾浩斯 |
| PlannerAgent | 112KB | 3种模式 + 4级降级 + 教材章节上下文 + 日计划分配 |
| ResourceAgent | 77KB | 差异检测(仅生成变更) + Review反馈修复 + 6类资源 |
| QuestionAgent | 27KB | 5题型 + 变式题 + deep_solve逐步解题 + 自一致性检查 |
| GradingAgent | 12KB | 5类错误分类 + 多维评分 + 自动复习路由 |
| ReviewAgent | 30KB | 7项质检(安全/事实性/一致性/覆盖/质量) → 不通过退回重试 |
| MultimodalAgent | 10KB | 12类任务关键词匹配 → ToolRegistry分发到对应Provider |

**Agent 注册机制**：`@register_agent("agent_id")` → `AgentFactory.get()` 延迟加载 → 共享 LLM 客户端

---

### 第 14 页 · 技术架构：RAG + LLM 工厂 + 多模态路由

**标题**：知识检索增强 + 多模型可插拔 + 多模态任务分发

**RAG 流水线**：

```
离线构建: 中文维基 → LlamaIndex SentenceSplitter(512/50)
  → HuggingFace text2vec-large-chinese(1024维) → FAISS IndexFlatIP

在线查询: Query → (短查询LLM扩词) → FAISS检索(top_k=5)
  → 有结果返回 → 无→课程目录回退→LLM兜底

降级: 索引损坏→预检跳过→不加载embedding模型→空结果+修复指引
```

**LLM 工厂**（`llm_factory.py`）：

```
用户请求 → user_ai_config(每用户DB) → 选Provider
  ├ DeepSeek ← 默认(对话/诊断/规划/生成)
  ├ Qwen ← VLM视觉问答
  ├ GLM/GPT-4o ← 备用
  └ 未配置 → AI_CONFIG_MISSING 友好提示
```

**多模态路由**（MultimodalAgent 关键词匹配→Provider）：

| 识别词 | Provider |
|--------|----------|
| "图片""截图" | Qwen Vision |
| "画""生成图" | 讯飞星火/Seedream |
| "做视频" | Wan Video |
| "演示算法" | code2video(Manim) |
| "做PPT" | 讯飞智文 AIPPT |
| "脑图" | LLM→Markmap |

---

### 第 15 页 · 技术架构：闭环评估引擎

**标题**：Assessment Loop —— 三个闭环构成的学习飞轮

**闭环一：学习主循环**

```
DiagnosisAgent → PlannerAgent → ResourceAgent → 学生学习
      ↑                                              ↓
      │                                    答题/测验/练习完成
      │                                              ↓
      │                                       GradingAgent
      │                                        (5类错误批改)
      │                                              ↓
      └── ProfileAgent ←── AssessmentLoop ←─────────┘
         (画像更新)         (触发再诊断)
```

**闭环二：质量保障**

```
ResourceAgent 生成 → ReviewAgent 7维质检
  ├→ 通过(安全/事实性/一致性/覆盖/质量/题目) → 发布
  └→ 不通过 → 退回 ResourceAgent 重试(≤2次,带修复反馈) → 再次 Review
```

**闭环三：推荐调度**

```
6信号源(未完成资源/低正确率/未完成练习/阶段未完成/高频薄弱/个人偏好)
  → RecommendationEngine → 个性化推荐列表

后台调度器:
  事件驱动 ── 每次测验提交即时触发
  周期复诊 ── 每10分钟扫描≥12h未评估的活跃会话
  SSE推送 ── 诊断更新/计划调整/推荐就绪/复习提醒
```

---

### 第 16 页 · 技术架构：数据与工程全貌 + 总结

**标题**：工程全貌 —— 40+ 服务模块 × 20+ 数据表

**左栏：服务模块分组**

| 组 | 核心服务 | 职责 |
|----|---------|------|
| Agent调度 | agent_factory, agent_service, langgraph_orchestrator, intent_router | 创建/缓存/编排/路由 |
| 会话状态 | conversation_state (75KB) | 会话、事实存储 |
| 画像系统 | profile_extractor, profile_v2 | 事实提取、话题级画像 |
| 诊断评估 | diagnosis_snapshot_service, assessment_loop (67KB), llm_assessment | 快照、调度、LLM评估 |
| 学习跟踪 | learning_tracker, recommendation_engine | 事件追踪、6信号推荐 |
| 资源生成 | resource_quality, content_quality_service, personalization_context, section_generated_resources, day_planner | 生成、审核、个性化 |
| LLM | llm_client, llm_factory, user_ai_config | 多模型统一接口 |
| 多模态 | multimodal_provider (87KB), multimodal_registry | Provider注册分发 |
| 知识检索 | search_client, course_catalog, knowledge_point_service | 搜索、课程目录 |
| 教材 | textbook_parser (48KB) | PDF解析、章节识别 |
| 安全 | content_safety | pyahocorasick+LLM |
| 工作流 | workflow_tasks | 长时任务+SSE进度 |

**右栏：核心数据表**

learners, sessions, messages, profile_snapshots, diagnosis_snapshots, diagnosis_evidence, learning_paths, planning_drafts, resources, learning_events, daily_tasks, questions, knowledge_points, student_questions, answer_records, quizzes, exam_sets, attempts, class_subjects, textbooks, assessment_states, user_ai_config, system_config

**总结公式**：
> EduAgent = **LangGraph 10 Agent 协同** × **RAG 知识增强** × **多模态生成路由** × **闭环评估引擎**

---

## 四、截图清单

| # | 页面 | 路由 | 展示内容 |
|---|------|------|---------|
| A | 学习中心首页 | `/` | 统计卡片 + 每日任务 + 学科列表 + 快捷入口 |
| B | 学习者画像 | `/profile` | 横幅 + 7维字段 + 知识点掌握区 |
| C | 学习路径 | `/path` | 左栏阶段列表 + 中栏任务卡片 + 右栏统计 |
| D | 知识图谱 | `/kg` | AntV G6 力导向图 + 搜索筛选 + 节点浮层 |
| E | 资源库 | `/resources` | 网格视图 + 筛选栏 + 多种资源卡片 |
| F | 智能对话 | `/chat` | 两张并列：①调用模式"生成思维导图"→流水线进度→SVG卡片；②"生成练习题"→题目列表卡片展开显示选项/答案/解析 |
| G | 资源生成 | `/generate` | 输入需求 + 类型选择 + 并行生成进度 |
| H | 练习中心 | `/practice` | 答题 + 批改结果（分数+错误类型+解析） |
| I | 讲稿学习 | `/lecture` | 讲稿模式 + 侧边栏 + Markdown正文 |

---

## 五、制作建议

1. **第 12 页架构总览用 draw.io 单独绘制**，导出高清 PNG 跨页
2. **第 13 页 LangGraph 图用 Mermaid 渲染**后截图嵌入
3. **所有截图区域先用虚线灰底占位**，PPT 做好后替换
4. **关键数字用计数动画**（10 Agent、20+ 页面、40+ 服务模块、20+ 数据表）
